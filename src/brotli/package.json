{
  "private": true,
  "name": "@nickbabcock/cloud-functions-brotli",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.mjs",
  "scripts": {
    "preinstall": "npm run build:wasm",
    "build": "npm run build:wasm && cp ../jomini/build.js . && node build.js",
    "build:wasm": "wasm-pack build -t web crate/",
    "publish": "wrangler publish",
    "test": "tsc"
  },
  "author": "Nick Babcock <nbabcock19@hotmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.4.0",
    "esbuild": "^0.14.27",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "crate": "file:crate/pkg"
  }
}
